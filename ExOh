function ExOh(str) { 
		var i,j, xCount = 0, oCount = 0;
  for(i = 0; i < str.length; i++){
  	 if ( str[i] === "x" ){ 
         xCount++;
    } else if (str[i] === "o") { 
         oCount++;
    } 
  }
  if (xCount === oCount){
  	  return true;
  }
  return false;
}

ExOh("xoxo");
